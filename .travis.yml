language: nodejs
node_js:
  - "11.0"
script:
  - echo "Deploying"
install: 
  - npm install -g firebase-tools
after_success:
  - npm run build && firebase deploy --only hosting --token $FIREBASE_TOKEN